{% extends "base.html" %}

{% block content %}
  <div class="span-15" id="article">
      <h1>{{ entry.headline }}</h1>
      <div class="articles-entry">
        {{ entry.article|safe }}
      </div>
      <div class="date">posted on <span id="red">{{ entry.created_on|date:"j M y" }}</span></div>
    </div>
{% endblock %}