{% extends "base.html" %}

{% block content %}
  <p><img src="/media/img/temp_highlights.png" /></p>
  <div id="news">
    <div class="entry">
      <h1>{{ entry.headline }}</h1>
      <div class="contents">
        {{ entry.article|safe }}
      </div>
      <div class="date">posted on <span id="red">{{ entry.created_on|date:"j M y" }}</span></div>
    </div><br /><br /><!-- need to add clearing -->
  </div>
{% endblock %}